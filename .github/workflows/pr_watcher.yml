name: PR watcher

on:
  pull_request

jobs:
  # Installs from the public registry only, so no auth is passed and a dependency that needs
  build:
    uses: ZaiusInc/node-sdk/.github/workflows/reusable-build.yaml@master
